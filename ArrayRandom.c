#include <stdio.h>
#include <stdlib.h>

int main ()
{
    int array[N]
    
    printf("Inserisci i valori che vuoi usare");
    scanf("&d", N);

    for(i=0; i<N; i++)
    {
        array[i]=rand()
        printf("a[%d] = %d\n", i, array[i]);
    }
}